{
  "name": "gwcfc-radar-parse",
  "private": true,
  "description": "SparkRadar's parsing stack, vendored: level2 and level3 are their src/parse folders (MIT, netbymatt upstream), dealias.js is the AtticRadar port, radar_worker.js is SparkRadar's worker with three GWCFC fixes marked inline.",
  "dependencies": {
    "buffer": "^6.0.3",
    "pako": "^2.1.0",
    "seek-bzip": "^2.0.0"
  }
}
